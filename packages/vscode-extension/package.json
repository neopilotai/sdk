{
  "name": "repolens-ai-vscode",
  "displayName": "RepoLens AI",
  "description": "AI-powered code review, coverage analysis, and productivity tools",
  "version": "0.1.0",
  "publisher": "repolens-ai",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "AI",
    "Programming Languages",
    "Other"
  ],
  "activationEvents": [
    "onLanguage:javascript",
    "onLanguage:typescript",
    "onLanguage:python",
    "onLanguage:go",
    "onLanguage:java",
    "onCommand:repolens.reviewPR",
    "onCommand:repolens.analyzeCoverage",
    "onCommand:repolens.generateTests"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "repolens.reviewPR",
        "title": "RepoLens: Review Pull Request",
        "description": "Review the current PR using AI"
      },
      {
        "command": "repolens.analyzeCoverage",
        "title": "RepoLens: Analyze Coverage",
        "description": "Analyze code coverage and identify uncovered code"
      },
      {
        "command": "repolens.generateTests",
        "title": "RepoLens: Generate Tests",
        "description": "Generate unit tests using AI to increase coverage"
      },
      {
        "command": "repolens.describePR",
        "title": "RepoLens: Describe PR",
        "description": "Generate PR description"
      },
      {
        "command": "repolens.improveCode",
        "title": "RepoLens: Improve Code",
        "description": "Get AI suggestions to improve selected code"
      },
      {
        "command": "repolens.askQuestion",
        "title": "RepoLens: Ask Question",
        "description": "Ask AI a question about the code"
      }
    ],
    "grammars": [
      {
        "language": "pr-description",
        "scopeName": "text.md.pr-description",
        "path": "./syntaxes/pr-description.JSON-tmLanguage"
      }
    ],
    "configuration": {
      "title": "RepoLens AI",
      "properties": {
        "repolens.githubToken": {
          "type": "string",
          "title": "GitHub Token",
          "description": "GitHub personal access token for API access"
        },
        "repolens.openAIKey": {
          "type": "string",
          "title": "OpenAI API Key",
          "description": "OpenAI API key for AI features"
        },
        "repolens.anthropicKey": {
          "type": "string",
          "title": "Anthropic API Key",
          "description": "Anthropic API key for Claude models"
        },
        "repolens.model": {
          "type": "string",
          "title": "AI Model",
          "default": "openai/gpt-4o",
          "description": "Default AI model to use"
        },
        "repolens.desiredCoverage": {
          "type": "number",
          "title": "Desired Coverage %",
          "default": 80,
          "description": "Target code coverage percentage"
        }
      }
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "@types/vscode": "^1.80.0",
    "typescript": "^5.5.0"
  }
}
